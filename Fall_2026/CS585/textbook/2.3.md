# 2.3 Electronic Mail in the Internet 

## Questions 

1. Does connecting directly to a Telnet server assure privacy, if both hosts 
   do it through commandline? If not why?

## 2.3.1 SMTP

- Simple Mail Transfer Protocol
- Old (1982): requires emails to be encoded into 7-bit ASCII (including images
  video files, etc) 
- No intermediary server's are used: if Alice sends to Bob and Bob's server is 
  down, the message waits in Alice's server
> "In ASCII jargon, each message ends with CRLF.CRLF, where CR and LF stand for carriage return and line feed, respectively."
- SMTP uses persistent connections: 
    > "For each message, the client begins the process with a new MAIL FROM: crepes.fr, designates the end of message with an isolated period, and issues QUIT only after all messages have been sent."
- Using Telentet to connect to an SMTP server:
    ```
    telnet serverName25
    ```
    - Will then recieve a 220 reply, can then issue the following SMTP
      commands: `HELO, MAIL FROM, RCPT TO, DATA, CRLF.CRLF, and QUIT`

## 2.3.2 Mail Message Formats 

- RFC 5322 defines teh header lines of messages:
    - Some keywords: `From:`, `To:`, `Subject:`
    - Different from SMTP commmands (these are part of the message, not the 
      handshaking protocol)

## 2.3.3 Mail Access Protocols

> "a typical user runs a user agent on the local host but accesses its mailbox stored on an always-on shared mail server. This mail server is shared with other users"
- How do you access your mail from server? SMTP is a push protocol, not a pull
  protocol:
    1. HTTP pull (Ex: Gmail)
    2. *Internet Mail Access Protocol* (IMAP): defined in RFC 3501 (Ex: 
       Outlook)
